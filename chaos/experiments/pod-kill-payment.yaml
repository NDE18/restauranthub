apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-payment
  namespace: restaurant
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - restaurant
    labelSelectors:
      app: service-payment
  duration: "60s"
