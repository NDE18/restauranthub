apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: java-services
  namespace: restaurant
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      stack: java
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
